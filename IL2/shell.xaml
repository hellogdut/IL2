<Window x:Class="SPEditorUI.Shell" Title="UI±à¼­Æ÷" Width="1000" Height="800" WindowStartupLocation="CenterScreen" WindowState="Maximized" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{DynamicResource ControlBackgroundBrush}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid Name="gridMenu" Grid.ColumnSpan="2">
      <ContentControl BorderThickness="0,0,0,1" RegionManager.RegionName="{x:Static com:RegionNames.UI_Menu}" />
    </Grid>
    <Grid Name="gridMainTool" Grid.Row="1" Grid.ColumnSpan="2">
      <ContentControl RegionManager.RegionName="{x:Static com:RegionNames.UI_MainTool}" />
    </Grid>
    <Grid Grid.Row="2">
      <ContentControl VerticalAlignment="Top" RegionManager.RegionName="{x:Static com:RegionNames.UI_ComTool}" />
    </Grid>
    <Grid Name="gridDockManager" Grid.Row="2" Grid.Column="1" />
    <Grid Name="gridStatus" Grid.Row="3" Grid.ColumnSpan="2">
      <ContentControl RegionManager.RegionName="{x:Static com:RegionNames.MainStatus}" />
    </Grid>
  </Grid>
</Window>